config:
  providers:
    aws:
      - name: aws-1
        access_key_id: foo
        secret_access_key: bar
        region: us-west-1
        zone: us-west-1a
      - name: aws-2
        access_key_id: foo2
        secret_access_key: bar2
        region: us-west-2
    vsphere:
      - name: vsphere-1
        vsphere_user: user
        vsphere_password: password
        vsphere_url: url
    virtualbox:
      - name: vsphere-1
        vsphere_user: user
        vsphere_password: password
        vsphere_url: url
