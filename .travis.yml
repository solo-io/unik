sudo: required
language: go
go:
  - 1.6

services:
  - docker

install:
  - go get -v github.com/onsi/ginkgo/ginkgo
  - go get -v github.com/onsi/gomega
  - export PATH=$PATH:$HOME/gopath/bin
  - travis_wait 120 make containers
  - go install

script: ginkgo -r --cover --trace --race --compilers=2
